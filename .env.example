# Copy this file to .env and fill values locally.
# Do not commit .env.

ALIO_API_KEY=
CLEANEYE_API_KEY=
OPENAI_API_KEY=
NOTION_TOKEN=

# Comma-separated browser origins allowed to call the HTTP MCP endpoint.
# Loopback origins and same-host origins are allowed by default.
MCP_ALLOWED_ORIGINS=
